# =========================
# Domaine & certificats TLS
# =========================
# Nom de domaine public pointant vers ton serveur (A/AAAA)
PUBLIC_FQDN=cstock.slegd.com
# Email utilisé par Let's Encrypt (Traefik)
TRAEFIK_ACME_EMAIL=you@example.com

# =========
# Sécurité
# =========
# Secret JWT (remplace par une valeur longue et aléatoire)
# Exemple pour en générer un :  openssl rand -base64 48
JWT_SECRET=please-change-me-to-a-long-random-secret

# ==========
# PostgreSQL
# ==========
POSTGRES_USER=cstock
POSTGRES_PASSWORD=cstockpass
POSTGRES_DB=cstockdb

# (optionnel, pratique pour utiliser Prisma en local depuis ta machine hôte,
# à condition d’exposer le port 5432 dans l’override local)
# DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:5432/${POSTGRES_DB}?schema=public

# =====
# MinIO
# =====
MINIO_ROOT_USER=cstockminio
MINIO_ROOT_PASSWORD=changeThisPassword
MINIO_BUCKET=cstock

# ===========================
# Frontend (confort / build)
# ===========================
# Cette valeur est l’URL publique de l’API qui sera injectée au BUILD du front
# (le docker-compose passe automatiquement: https://${PUBLIC_FQDN}/api)
# Tu peux la laisser en commentaire ou la mettre explicitement si tu rebuilds à la main :
# VITE_API_BASE=https://cstock.slegd.com/api

# (optionnel) Ports "confort" pour tes overrides locaux (non utilisés en prod)
API_PORT=3000
WEB_PORT=5173
